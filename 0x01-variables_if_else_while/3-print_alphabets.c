#include <stdio.h>
/**
 * main - main Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	char D;

	printf("c: ");
	for (c = 'c'; c <= 'z' c++) { 
		printf("%c", c);
	}
	printf("\n D: ");
	for ( 'C' ; <= 'D'; D++) {
		printf("%c",c);
		}

	printf('\n');
	
	return (0);
}

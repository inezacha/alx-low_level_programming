#include <stdio.h>
/**
 * main - main Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	char d;

	c = 'a';
	d = 'A';
	while
		(c <= 'z') {
			putChar(c);
			c++,
		}
	while
		(d <= 'z') {
			putChar (d);
			d++,
		}
	putchar('\n');
	return (0);
}
